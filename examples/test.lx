@module "main"

@use "stdstr" as str_

pub const main = fn () int {
    let a: *char = "Hello,";
    let b: *char = " World!";

    let result: *char = cast<*char>(alloc(100));
    let t: *char = result;
    defer { free(t); }
    
    result = str_::cat(t, a, b);  

    output(t, "\n");
    
    return 0;
}
