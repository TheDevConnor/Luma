@module "main"

@use "math" as math

pub const main = fn () int {
   loop [i: int = 0](i <= 100000000) : (++i) {
    math::sin((cast<float>(i) * 2 * math::PI) / cast<float>(100));
   }

   return 0;
}
// time 2.725
