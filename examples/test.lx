@module "main"

@use "stdstr" as str_

pub const main = fn () int {
    let a: *char = "Hello,";
    let b: *char = " World!";

    let result: *char = cast<*char>(alloc(100));
    defer { free(result); }
    
    result = str_::cat(result, a, b);  

    output(result, "\n");
    
    return 0;
}
