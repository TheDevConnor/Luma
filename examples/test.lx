@module "main"

// @use "math" as math

pub const main = fn () int {
  let x: *int = cast<*int>(alloc(sizeof<int> * 10));
  defer { free(x); }

  loop [i: int = 0](i < 10) : (++i) {
    x[i] = i;
  }

  loop [i: int = 0](i < 10) : (++i) {
    output(x[i], "\n");
  }

  return 0;
}