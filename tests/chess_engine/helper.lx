@module "helper"

pub const Move -> struct {
  from_col: int,
  from_row: int,
  to_col: int,
  to_row: int
};

pub const Board -> struct {
  starting_fen: *char,
  length_fen: int,
  squares: *char, 
};
